{
  "manifest_version": "0.4",
  "name": "pm-server",
  "display_name": "PM Server (Lens)",
  "version": "0.8.0",
  "description": "Read-only project-management Lens for Claude Desktop/Cowork. Surfaces tasks, blockers, memory, and dashboards from a project's .pm/ store; writes are structurally disabled.",
  "long_description": "PM Server is a project-management MCP server. This .mcpb bundle ships it in Lens mode (PM_LENS=1): only the read-only tool allowlist (pm_status, pm_next, pm_tasks, pm_blockers, pm_recall, pm_memory_search, pm_memory_stats, pm_dashboard, pm_risks, pm_velocity, pm_list, pm_knowledge_query, pm_workflow_status, pm_workflow_list, pm_workflow_templates) is registered. Mutators and subprocess paths are structurally unreachable (ADR-015/017/018). SQLite is opened with mode=ro&immutable=1 so the Desktop/Cowork host never creates -wal/-shm sidecars in another project's .pm/ (PMSERV-080).",
  "author": {
    "name": "flc-design",
    "url": "https://github.com/flc-design"
  },
  "homepage": "https://github.com/flc-design/pm-server",
  "repository": {
    "type": "git",
    "url": "https://github.com/flc-design/pm-server"
  },
  "license": "MIT",
  "keywords": [
    "claude-desktop",
    "claude-cowork",
    "project-management",
    "mcp",
    "lens",
    "read-only"
  ],
  "server": {
    "type": "uv",
    "uv": {
      "package": "pm-server",
      "command": "pm-server",
      "args": ["serve"]
    },
    "mcp_config": {
      "env": {
        "PM_LENS": "1",
        "PM_DESKTOP_WRITE": "1"
      }
    }
  },
  "compatibility": {
    "platforms": ["macos", "linux", "windows"],
    "runtimes": {
      "python": ">=3.11"
    }
  },
  "user_config": {
    "project_path": {
      "type": "string",
      "title": "Project root",
      "description": "Absolute path to a project directory containing .pm/project.yaml. If omitted, pm-server falls back to PM_PROJECT_PATH env, then to single-entry auto-pick from ~/.pm/registry.yaml under PM_LENS, then to a cwd walk-up. Set this explicitly when more than one project is registered.",
      "required": false
    }
  }
}
